# SEO and Rank Math

Rank Math is an integration, not a replacement.

When Rank Math is active and `defer_to_rank_math` is on (default), the internal SchemaGraph does not print JSON-LD.

Fallback titles and meta descriptions run only when Rank Math is absent.
