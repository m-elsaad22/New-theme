# Schema

`MahmoudElsaad\Core\SEO\SchemaGraph` emits a `@graph` with Organization/LocalBusiness, WebSite+SearchAction, Service, Article/BlogPosting, FAQPage, ContactPage, AboutPage.

Duplicate nodes are dropped by type+id.
