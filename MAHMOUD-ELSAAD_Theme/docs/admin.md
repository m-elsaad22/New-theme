# Admin Control Center

Screen: `admin.php?page=mes-control-center`

Mobile-first shell with side nav, bottom nav on small screens, Ctrl/K command search, dashboard cards, design tokens, AI keys, analytics JSON, and migration runner.

REST under `mes/v1` requires `manage_options` / `mes_manage_platform` except public search and click tracking.
